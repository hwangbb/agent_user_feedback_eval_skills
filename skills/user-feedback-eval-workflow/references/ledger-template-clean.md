# 列表模板

每完成一条 case，保留一个简短条目：

```text
当前改编列表
1. case_label=...
   issue_type=...
   case_format=...
   rewrite_status=accepted / needs_revision
   judge_status=not_run / valid / invalid
   score=...
   main_takeaway=...
   next_action=...
```

如果用户切换了评测集格式，把 `case_format` 一起更新。
